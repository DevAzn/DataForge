---
name: skill-evolve
description: >
  Promote repeated PV_DataForge workflows into project skills and keep AGENTS.md
  in sync. Use when the user says "promote to skill", "we keep doing this",
  "nail this down", "update the skill", "create a skill for…", or "/skill-evolve".
---

# Skill evolve (PV_DataForge)

1. Name the loop (2–5 words); list steps or the bug that must not recur  
2. Check AGENTS.md skill map — reuse or create `pv-*` under `.grok/skills/`  
3. Write/update `SKILL.md` (frontmatter name + triggers + actionable body)  
4. Sync AGENTS.md map + session changelog  
5. Tell user path + `/name` + what it prevents  

Prefer project scope. Thin skills; link CONTEXT for long policy.
