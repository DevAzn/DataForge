# Core (Python 3.12+; prefer 3.14 when available)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
pydantic>=2.9.0
pyyaml>=6.0.2

# Optional bulk (uncomment if needed):
# polars>=1.0.0
# numpy>=2.0.0
