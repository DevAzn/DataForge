# ── Python ──────────────────────────────────────────────────────────
.venv/
venv/
backend/.venv/
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
# Only app build outputs — never strip package trees under node_modules
# Ship onedir under dist/DataForge (exe + _internal + data). Versioned zips stay local / Releases.
/dist/*
!/dist/release-notes*.md
!/dist/DataForge/
!/dist/DataForge/**
/build/
frontend/dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
*.cover

# ── Node / Vue ──────────────────────────────────────────────────────
# frontend/node_modules/ is tracked so clones can run without npm install
!frontend/node_modules/
!frontend/node_modules/**
npm-debug.log*
yarn-error.log*
.pnpm-store/

# ── Runtime data (local only; desktop onedir data/ is tracked) ──────
/data/
data/
!/dist/DataForge/data/
!/dist/DataForge/data/**
*.sqlite
*.sqlite3
*.db
!/dist/DataForge/data/**/*.sqlite
!/dist/DataForge/data/**/*.sqlite3
!/dist/DataForge/data/**/*.db

# ── Secrets & env ───────────────────────────────────────────────────
.env
.env.*
!.env.example

# ── OS / editor ─────────────────────────────────────────────────────
.DS_Store
Thumbs.db
Desktop.ini
.idea/
.vscode/
*.swp
*.swo
*~

# ── Logs & temp ─────────────────────────────────────────────────────
*.log
tmp/
temp/
*.tmp


# ── Docs: README + agent/Duo instruction files that must ship to GitLab ─
*.md
!README.md
!AGENTS.md
!.gitlab/**/*.md
!skills/**/*.md

# ── Local-only tooling (keep on disk; do not commit) ────────────────
.github/
.grok/
GOAL.md
SCORECARD.md
GOALS.md
*.session.md
CONTEXT.md
GROK_BUILD_SETUP.md

# PowerShell: ignore ad-hoc leftovers, keep packaging/start scripts
*.ps1
!scripts/build-exe.ps1
!scripts/start-desktop.ps1
!scripts/publish-desktop-release.ps1
scripts/cleanup-electron-remnants.ps1
scripts/finalize-dataforge-path.ps1
scripts/finalize-pv-path.ps1

# NOTE: .gitlab/ and AGENTS.md ARE intended for commit (GitLab Duo + CI).
# Do not add a blanket ".gitlab/" ignore rule.

# Desktop exe packaging
build/pyinstaller/
